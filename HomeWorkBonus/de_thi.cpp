#include<stdio.h>
#include<conio.h>
#include<math.h>
#include<string.h>
int main()
{
	/*int n,i;
	float s=1;
	do{
		printf("Nhap n: ");
		scanf("%d",&n);
		
	}
	while(n<=0);
	for(i=1;i<=(2*n-1);i++){
		if(i%2!=0){
			s*=i;
		}
	}
	printf("\nKet qua la: %.2f",s);*/
	double x;
	
	double sau;
	int truoc;
	truoc=(fabs(x)/1);
		sau=fabs(x)-truoc;
		return truoc;
		return sau;

	
		printf("Nhap x: ");
		
		scanf("%lf",&x);
	
		
		char str[50];
	strcpy(str, "0.34");

   int y = strlen(str);
		
	printf("\nKet qua la: %s %d",str,y);
	
		
	return 0;
}

	
